#numbers from n to 1 and 1 to n

def numbers(n):
    if n>0:
        print(n,end=" ")
        numbers(n-1)
        print(n,end=" ")
n=5
numbers(n)

#by using indexing
def rec_1_N(index,n):
    if index == n+1:
        return
    print(index,end=" ")
    rec_1_N(index+1,n)
rec_1_N(1,5)
